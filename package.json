{
  "name": "careersharp",
  "type": "module",
  "version": "1.0.0",
  "description": "A job aggregator that allows you to save jobs and generates recommendations for how to customize your application based on the job description content.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "serve": "wds --open / --watch --node-resolve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Branden Wheeler",
  "license": "ISC",
  "dependencies": {
    "lit": "^3.1.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.2"
  }
}
